[project]
name = "brokerops-email-smtp"
version = "0.1.0"
description = "EmailSender adapter that sends over SMTP"
requires-python = ">=3.12"
dependencies = [
    "brokerops-core",
]

[tool.uv.sources]
brokerops-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
