[project]
name = "brokerops-email-stub"
version = "0.1.0"
description = "Stub email provider: EmailPort adapter + MCP server + zero-credential double"
requires-python = ">=3.12"
dependencies = [
    "brokerops-core",
    "fastapi>=0.115",
    "httpx>=0.28",
    "mcp>=1.2",
    "uvicorn[standard]>=0.32",
]

[project.scripts]
mcp-server-email-stub = "brokerops_email_stub.mcp_server:main"

[tool.uv.sources]
brokerops-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
