[tool.uv.workspace]
members = ["api", "core", "integrations/mls_reso"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.mypy]
strict = true

[tool.pytest.ini_options]
testpaths = ["api/tests", "core/tests", "integrations/mls_reso/tests"]
asyncio_mode = "auto"
